%section.nav
  %article.nav-left
    %h1
      = link_to('Messenger', root_path)

  %article.nav-right
    - if current_user
      = link_to('Log out', destroy_user_session_path,  :method=>'delete')
    - else
      = link_to('Log in', new_user_session_path)

